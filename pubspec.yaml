name: water_jug
description: Experiments with water and jugs
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  intl: ^0.17.0
  yalo_assets:
    path: .yalo_assets
  yalo_locale:
    path: .yalo_locale
  flutter_svg: ^1.0.3
  anitex: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  yalo: ^2.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/